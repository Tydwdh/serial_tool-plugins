# 二进制：zip 产物，绝不做换行转换
*.zip binary

# 文本默认用 LF（跨平台一致）
* text=auto eol=lf

# Windows 脚本保持 CRLF（PowerShell 对编码/换行较宽容，但本地习惯 CRLF）
*.ps1 text eol=crlf

# JSON / Markdown / schema 统一 LF
*.json text eol=lf
*.md text eol=lf
*.schema.json text eol=lf
